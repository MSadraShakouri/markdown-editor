---
title: نمونهٔ جامع
tags: [تست، مارک‌داون]
---

# سرفصل یک

React یک کتابخانهٔ رابط کاربری است؛ این پاراگراف با یک واژهٔ لاتین شروع می‌شود، پس
جهت آن باید درست تشخیص داده شود و نقطهٔ آخر در جای درست قرار بگیرد.

This paragraph is pure English inside a Persian document: it must stay left-aligned
even though the page is RTL.

یک خط فارسی با `inline code` در میان آن — پرانتزها و ویرگول‌ها باید سر جایشان بمانند،
مثل `useState(count)` و `arr[0]`.

## فهرست‌ها

- مورد ساده
- مورد توکار
  - زیرمورد با **تیره** و *کج*
- [x] کار انجام‌شده
- [ ] کار باقی‌مانده
- [X] با حرف بزرگ هم کار می‌کند

1. اول
2. دوم
   1. دوم-یکم
3. [ ] وظیفه در فهرست شماره‌دار

### جدول

| نام | قیمت | وضعیت |
|:-----|:----:|------:|
| کتاب | ۱۲۵٬۰۰۰ | موجود |
| `a\|b` (پایپ گریخته) | ۰ | ناموجود |
| [لینک](https://example.com) | ۱۰ | **مهم** |

بین ستون‌ها می‌توان از `\|` برای نوشتن پایپ استفاده کرد.

### نقل‌قول تو در تو

> نقل‌قول سطح یک
> > نقل‌قول سطح دو
> > با `کد` هم

### پانویس

متن با پانویس[^یادداشت] و ارجاع دوباره به همان[^یادداشت] و یکی دیگر[^note-1].

[^یادداشت]: متن پانویس فارسی با برچسب غیرعددی.
[^note-1]: A footnote with an ASCII label.

### کد

```js
// کامنت فارسی در کد — بلوک کد باید چپ‌چین باشد
const price = 125_000;
console.log(`قیمت: ${price}`);
```

کد تورفتگی (indented):

    def salaam():
        print("سلام")

### لینک‌ها و تصویر

<https://example.com> · www.example.com · https://bare.example.com/x

[لینک مرجع][ref] · [کوتاه][] · [مستقیم](https://example.org)

![تصویر نسبی](images/diagram.png)

[ref]: https://example.com
[کوتاه]: https://example.org

### خط‌کشی و شکستن خط

---

خط اول با دو فاصله در انتها  
خط دوم بعد از hard break

خط بعدی با بک‌اسلش در انتها\
و ادامهٔ همان پاراگراف نیست.

### گریزها

\*ستاره واقعی\* و \_زیرخط واقعی\_ و \# بدون سرفصل

### اچ‌تی‌ام‌ال خام

<details>
<summary>بیشتر بخوانید</summary>

این بلوک `<details>` باید در پیش‌نمایش کار کند، اما `<script>`، `<form>`،
`onerror` و `style` نباید اجرا شوند.

</details>

---

## برچسب تاریخ: ۱۴۰۴/۰۷/۰۳
