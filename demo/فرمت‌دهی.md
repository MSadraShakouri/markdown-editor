# نمایش قابلیت‌ها

## تأکید

**تیره**، *کج*، ~~خط‌خورده~~، `کد درون‌خطی` و یک ترکیب **تیره با *کج* درونش**.

## فهرست‌ها

- مورد ساده
- مورد توکار
  - زیرمورد
- [x] کار انجام‌شده
- [ ] کار باقی‌مانده

1. اول
2. دوم
   1. دوم-یکم

## جدول

| نام | قیمت | وضعیت |
|:-----|:----:|------:|
| کتاب | ۱۲۵٬۰۰۰ | موجود |
| دفتر | ۴۵٬۰۰۰ | ناموجود |

## نقل‌قول

> نقل‌قول سطح یک
> > نقل‌قول سطح دو، با `کد` درونش

## کد

```js
// کامنت فارسی داخل کد فارسی — بلوک کد باید چپ‌چین بماند
const قیمت = 125_000;
console.log(`مبلغ: ${قیمت} تومان`);
```

کد تورفتگی‌دار هم پشتیبانی می‌شود:

    SELECT نام FROM کتاب‌ها WHERE موجود = 1;

## پانویس و پیوند

متن با پانویس[^۱] و یک [پیوند نمونه](https://example.com).

[^۱]: متن پانویس در پایین سند جمع می‌شود.

## پرانتزها و جهت متن

مقایسهٔ `useState(count)` و `arr[0]` داخل جملهٔ فارسی باید درست بماند.
